{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "cd /app && bun --version && npm run start --workspace=@live-traffic/api",
    "restartPolicyMaxRetries": 5,
    "restartPolicyWindowMs": 60000
  }
}
